import type { CommandHistoryEntry, SystemStatus } from '../types/lunex';
export function VoiceInputPanel({ recording }: { recording: boolean }) { return <div className="panel"><h3>Voice Input</h3><p>{recording ? 'Recording placeholder active. Click again to stop and insert transcription.' : 'Push-to-talk, wake word, and double-clap activation are scaffolded.'}</p></div>; }
export function RecentFilesPanel() { return <div className="panel"><h3>Recent Files</h3><ul><li>project-plan.md</li><li>meeting-notes.txt</li><li>budget-draft.xlsx</li></ul></div>; }
export function QuickActionsPanel({ onWake, onClap }: { onWake: () => void; onClap: () => void }) { return <div className="panel"><h3>Quick Actions</h3><button onClick={onWake}>Simulate “Hey Lunex”</button><button onClick={onClap}>Simulate Double Clap</button><button>Open command center</button></div>; }
export function SystemStatusPanel({ status }: { status: SystemStatus }) { return <div className="panel metrics"><h3>System Status</h3><span>OS {status.os}</span><span>CPU {status.cpu}</span><span>RAM {status.ram}</span><span>Disk {status.disk}</span><span>Network {status.network}</span></div>; }
export function AIMetricsPanel() { return <div className="panel metrics"><h3>AI Metrics</h3><span>Router ready</span><span>Web search tool planned</span><span>Voice placeholder</span></div>; }
export function TasksPanel() { return <div className="panel"><h3>Tasks / Reminders</h3><p>No live reminders yet. Task storage is ready in SQLite schema.</p></div>; }
export function CommandHistoryPanel({ entries }: { entries: CommandHistoryEntry[] }) { return <div className="panel history"><h3>Command History</h3>{entries.map((entry) => <article key={entry.id}><strong>{entry.label}</strong><small>{entry.timestamp}</small><p>{entry.detail}</p></article>)}</div>; }
